%h2 Add Jumpers

= simple_form_for [randomizer, Jumper.new(:randomizer => randomizer)], :remote => true, :validate => true do |f|
  = render :partial => "jumpers/form", :locals => {:f => f, :randomizer => randomizer}
  .loading
    = image_tag "loading.gif"
  .submit
    = f.submit 'add'
